#include <stdio.h>
int main(void)
{
	char x,y;
	x='A';
	y='a';
	int z= 65;
	printf("the value of x(decimal): %d, \n the value of x(hexadecimal): %x \n", x,x);
	printf("the value of y(decimal): %d, \n the value of y(hexadecimal): %x \n ",y,y);
	printf("the character value of 25 is: %c\n", z);
	return 0;
}
